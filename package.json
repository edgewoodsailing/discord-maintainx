{
  "name": "discord-maintainx",
  "private": true,
  "type": "module",
  "description": "Bridge that lets Discord users file MaintainX work requests via a /request slash command",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc -p tsconfig.json && tsc -p tsconfig.scripts.json",
    "register-commands": "tsx scripts/register-commands.ts",
    "list-assets": "tsx scripts/list-assets.ts",
    "subscriptions": "tsx scripts/subscriptions.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
